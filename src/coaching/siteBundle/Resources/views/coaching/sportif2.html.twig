{# coaching/app/resources/views/layout.html.twig #}
{% extends "::layout.html.twig" %}
{% block body %}
	<h2>Espace Sportif</h2>
	
		<div class="info_sportif">
			<div class="physique">
				<label>Nom</label>
				{{ user.nom }}
				<label>Prénom</label>
				{{ user.prenom }}
			</div>
			<div class="physique">
				
				<label>Taille</label>
				{{ sportif.taille }}
				<label>Poids</label>
				{{ sportif.poids }}
			</div>
			<div class="sexe">
				<label>Sexe</label>
				{{ sportif.sexe }}
			</div>
			<div class="naissance">
				<label>Date de Naissance</label>
				{{ sportif.DateNaissance|date('d/M/Y') }}
			</div>
			<div class="niveau">
				<label>Niveau</label>
				{{ sportif.niveau }}
				<label>Entrainement</label>
				
			</div>
			<div class="lien">
				<a href="{{path('sportif_modif')}}">Modification</a>
				<a href="{{path('sportif_suppr')}}">Suppression</a>
			</div>
		</div>
	
	<div class="navigation">
	<a href="{{path('accueil')}}">Retour</a>
	</div>
{% endblock %}